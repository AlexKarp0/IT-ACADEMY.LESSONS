package CarFactoryPattern;

public class SmallCar extends Car{
    public SmallCar(String model, String number, String color, double maxSpeed) {
        super(model, number, color, maxSpeed);
    }
}
