package CarFactoryPattern;

public class SportCar extends Car{
    public SportCar(String model, String number, String color, double maxSpeed) {
        super(model, number, color, maxSpeed);
    }
}
