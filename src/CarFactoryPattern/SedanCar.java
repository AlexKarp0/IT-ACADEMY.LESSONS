package CarFactoryPattern;

public class SedanCar extends Car{
    public SedanCar(String model, String number, String color, double maxSpeed) {
        super(model, number, color, maxSpeed);
    }
}
